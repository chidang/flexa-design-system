{
  "slug": "password-input",
  "tagline": "Password field with a visibility toggle and optional strength meter.",
  "whenToUse": "Use for any secret entry — sign-in, sign-up, change-password — always setting autoComplete so managers behave. Add strengthMeter on new-password flows; the visibility toggle stays available even when the field is disabled."
}
