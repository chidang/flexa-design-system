{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Cố ý KHÔNG có lib DOM: core là pure TS, không được đụng DOM/platform API.
    "lib": ["ES2022"],
    "types": []
  },
  "include": ["src/**/*.ts", "tests/**/*.ts"]
}
