{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Cố ý KHÔNG có lib DOM: FDS là pure TS (data + pure fn), không đụng DOM/platform API.
    "lib": ["ES2022"],
    "types": []
  },
  "include": ["src/**/*.ts", "src/**/*.json", "tests/**/*.ts"]
}
